OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

# Optional: create a persistent OpenAI vector store and set its ID here.
# If omitted, the app creates a temporary runtime vector store that may reset on serverless cold starts.
OPENAI_VECTOR_STORE_ID=
